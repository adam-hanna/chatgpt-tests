module.exports = {
    preset: 'ts-jest',
    testEnvironment: 'node',
    testMatch: ['**/*.test.ts'], // Look for .test.ts files
    moduleFileExtensions: ['ts', 'js'], // Allow importing .ts and .js files
};